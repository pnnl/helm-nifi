service:
  type: "ClusterIP"
  httpsPort: "8443"
  processors:
    enabled: true
    ports: []
    
zookeeper:
  enabled: true

properties:
  isNode: true
  webProxyHost: nifi.default.svc.cluster.local:8443
  clusterSecure: true
  httpsPort: 8443

replicaCount: 2

registry:
  enabled: false

certManager:
  enabled: true
